MANUALLY PUSH IMAGES TO ECR

Install Jenkins
Install docker
Install AWS CLI

CREATE IAM USER
aws configure (update access key and secrete access key)


# install aws cli on docker sever
``````
apt install unzip
curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install
./aws/install -i /usr/local/aws-cli -b /usr/local/bin
aws –version
``````

# aws configure (update access key and secrete access key)
# aws ecr describe-repositories
# View push commands : execute commands
It will deployed to petclinic-ECR repository
